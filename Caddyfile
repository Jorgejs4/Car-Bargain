{$APP_DOMAIN} {
    encode gzip

    handle /api/* {
        reverse_proxy api:8000
    }

    handle /internal/* {
        reverse_proxy api:8000
    }

    handle /health {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}
